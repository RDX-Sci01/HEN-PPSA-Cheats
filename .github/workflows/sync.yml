name: Sync HEN MC4 PPSA

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Clone upstream
        run: |
          git clone --depth 1 https://github.com/TeeKay87/HEN-Cheats-Collection.git upstream

      - name: Build filtered collection
        run: |
          set -e

          rm -rf cheats
          mkdir -p cheats/mc4

          SRC="upstream/cheats"

          # Copy PPSA MC4 cheat files
          find "$SRC/mc4" \
            -type f \
            -name 'PPSA*' \
            -exec cp {} cheats/mc4/ \;

          # Filter PPSA entries from index file (if it exists)
          if [ -f "$SRC/mc4.txt" ]; then
            grep '^PPSA' "$SRC/mc4.txt" > cheats/mc4.txt || true
          fi

      - name: Commit and push changes
        run: |
          set -e

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add cheats

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Sync upstream PPSA cheats"
          git push origin HEAD
